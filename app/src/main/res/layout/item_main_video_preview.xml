<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Video container for playback -->
    <FrameLayout
        android:id="@+id/playerView"
        android:tag="multi_video_item_player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black"
        android:layout_centerInParent="true"
        android:gravity="center">
        
        <!-- TextureView for video display with proper scaling -->
        <TextureView
            android:id="@+id/videoSurface"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:scaleType="matrix" />
            
    </FrameLayout>

    <!-- Video thumbnail ImageView (fallback) -->
    <ImageView
        android:id="@+id/videoThumbnail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerInside"
        android:adjustViewBounds="true"
        android:visibility="gone" />

    <!-- Play button overlay -->
    <ImageView
        android:id="@+id/playPauseButton"
        android:tag="multi_video_item_play_pause"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_centerInParent="true"
        android:background="@drawable/black_background_hover"
        android:padding="18dp"
        android:src="@drawable/play"
        android:clickable="true"
        android:focusable="true"
        app:tint="@color/white" />

</RelativeLayout>