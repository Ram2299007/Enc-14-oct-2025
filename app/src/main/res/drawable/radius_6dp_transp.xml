<shape xmlns:android="http://schemas.android.com/apk/res/android">
   <!-- Fill color -->
    <stroke
        android:width="1dp"
        android:color="@color/atoz" />
    <corners android:radius="20dp" />
    <solid android:color="@color/atoz"/>
    <!-- Corner radius -->
</shape>
